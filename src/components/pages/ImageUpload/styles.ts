import styled from 'styled-components'
import { space } from '../../../styling/tokens'

export const ButtonsContainer = styled('div')`
  display: flex;
  justify-content: space-between;
`

export const PredictButtonSection = styled('div')`
  display: flex;
  gap: ${space[1]};
`
